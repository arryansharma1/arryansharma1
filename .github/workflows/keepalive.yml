name: ⏰ Keepalive
on:
  schedule:
    - cron: '0 18 * * *'  # 30 min before your GIF rotation
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: gautamkrishnar/keepalive-workflow@v2
